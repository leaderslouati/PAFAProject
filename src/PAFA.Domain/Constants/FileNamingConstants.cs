namespace PAFA.Domain.Constants;

/// <summary>
/// Centralised constants for PARR file naming convention enforcement.
/// Used by FileNameValidator and FolderPathValidator.
/// </summary>
public static class FileNamingConstants
{
    // ?? Prohibited characters in file names ???????????????????????????
    // Any file name containing one of these characters is rejected (NAME-001).
    public static readonly char[] ProhibitedChars = ['*', '/', '?', ':', '<', '>', '|', '"', '\\'];

    // ?? Accepted month token formats ??????????????????????????????????
    // Used by DateOnly.TryParseExact inside FileNameValidator (NAME-003).
    public static readonly string[] MonthFormats = ["MMM", "MM", "MMMMyy", "MMMyy"];

    // ?? Folder structure constraints ??????????????????????????????????
    public const int MinYear = 2020;
    public const int MaxYear = 2040;
}
