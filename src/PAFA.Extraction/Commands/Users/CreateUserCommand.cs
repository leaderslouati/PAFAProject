using MediatR;

namespace PAFA.Extraction.Commands.Users;

// ?? Request DTO ????????????????????????????????????????????????????????????
// Represents the payload sent by the PAFA Administrator to create a new user.

/// <summary>
/// Command to create a new PAFA user account and assign roles.
/// Restricted to callers with the PafaAdmin role.
/// </summary>
public record CreateUserCommand(
    string FirstName,
    string LastName,
    string Email,
    string? JobTitle,
    string? Department,
    List<int> RoleIds,
    /// <summary>Id of the admin performing the action — extracted from the JWT claim.</summary>
    string AdminId
) : IRequest<CreateUserResult>;

// ?? Result ?????????????????????????????????????????????????????????????????

public record CreateUserResult(
    bool Success,
    Guid? UserId,
    string? ErrorMessage
);
